# PROMPT — 单盆位墙面种植单元(喂给 Claude Code)

> 用法:把本文件放进项目根目录,在 Claude Code 里说「读 CLAUDE.md、SPEC.md、PROMPT.md,按 PROMPT 改 wall_planter_v2.scad」。
> 以现有 `wall_planter_v2.scad` 为基础**改造**,不要从零重写。每改一处按 CLAUDE.md 的闭环渲染 PNG 并查看,几何确认后导 STL。

## 目标
做一个**参数化、可 3D 打印、可堆叠/横拼**的「单盆位」墙面种植单元。
单元本身是一个**斜置的花盆托座(箱体)**,里面**斜放一个可拆卸的锥形花盆**;
顶部加水后水经盆底排水孔进入箱体底部储水层,超过水位逐层往下淌。

## 剖面机制(逐元素,来自参考剖面图)
箱体为侧剖,背板在后(贴墙侧),整体结构:

- **斜置锥形花盆腔**:箱体内主腔是一个**向后下方倾斜**的圆台(锥形)空腔,用来容纳可拆花盆。开口朝**前上方**(植物从这里长出、土朝外不易撒),盆底(窄端)落在**后下方**。倾角约 25–30°(设为参数 `pot_tilt`)。
- **植物苗**:从开口长出,叶子向前外舒展——开口必须在前面板上完整露出(正圆/椭圆开口)。
- **土壤层**:花盆内的种植土,占据锥形腔大部分。
- **透气孔**:箱体**上部侧壁/背壁**靠近盆口根部区域开若干小孔,给根系透气。位置在开口上沿一圈。
- **排水孔**:花盆**最低点(后下方盆底)**有孔,水向下穿过箱体进入底部储水层。
- **储水层**:箱体**最底部**的浅腔,承接排水孔下来的水;侧/底有**溢流孔**,超过水位则向下一层单元淌(逐层灌溉)。
- **顶部缺口**:箱体顶后沿有一处半圆缺口(参考图可见),给上方堆叠单元的水路/盆口让位,并辅助透气。
- **底层堵塞**:最底层单元的溢流孔需可被堵塞(留一个可塞橡胶塞的孔即可,不必建塞子),避免水滴到地面。

## 硬约束
- **打印机**:拓竹 P2S,有效打印体积 256×256×256mm;实际按 **≤ 240mm/轴** 设计留余量(左前角有禁打区)。
- **单元尺寸**:因花盆较大,**单次只打 1 个盆位**(`cols=1, rows=1`)。单元三维都必须 ≤ 240mm。组墙靠物理堆叠+横拼,不在单件里做多盆。
- **安装**:仅堆叠(`仅堆叠`)。**不要**加挂墙件、不要加底座脚。只需上下/左右的**堆叠定位**(定位销+孔或浅榫),保证摞起来不滑。
- **灌溉**:逐层淌水(drip-through),**不做**密封储水。储水层只需承接+溢流到下层,允许 FDM 轻微渗水。
- **材料/壁厚**:默认壁厚 2.6mm;免支撑优先。

## 可拆花盆尺寸(托座腔体按此 + 间隙)
托座的锥形腔要能**放进**这个花盆(花盆另购或另打,本单元先做托座):
- 盆高:**148 mm**
- 上开口外径:**131.2 mm**
- 盆底外径:**93.2 mm**(上大下小的圆台)

腔体取**单边 ~1.5mm 间隙**(直径方向 +3mm):腔上口径 ≈ 134.2mm,腔底径 ≈ 96.2mm,腔深 ≈ 150mm。
OpenSCAD 用 `cylinder(h=pot_depth, d1=底径, d2=上口径)` 做圆台,**宽端(d2)在开口/前,窄端(d1)在后下**。

## 需要从 wall_planter_v2.scad 改的点
1. **花盆腔改为圆台(锥形)**:把原来直圆柱 cup 换成 `d1/d2` 圆台,宽端朝开口。新增参数:`pot_top_d=131.2`、`pot_bot_d=93.2`、`pot_height=148`、`fit_clear=1.5`。`pot_d` 用 `pot_top_d+2*fit_clear` 派生。
2. **网格降为 1×1**:`cols=1, rows=1`。校核单元 W/D/H 三维都 ≤ 240,`echo` 出来确认。
3. **保留并对齐**:前面板正圆开口(用腔上口径)、开口上沿一圈**透气孔**、盆底**排水孔**、底部**储水层 + 溢流孔**。
4. **堆叠定位**:顶/底定位销+孔保留;左右用浅榫或销做横拼定位(不需要承重燕尾,因为是堆叠)。
5. **底层堵塞孔**:溢流孔尺寸设成可塞标准橡胶塞(留参数 `plug_d`,默认 ~12mm),不建塞子本体。
6. **杯体不穿背**:沿用 `mod_d` 含 `pot_d/2*sin(pot_tilt)` 容差项的写法(见 CLAUDE.md 已知坑)。

## 验收清单(逐项渲图确认)
- [ ] 正前视图(`rz=180`):开口为完整圆台口,植物能从中长出;开口上沿有透气孔。
- [ ] 立体图:花盆腔明显向后下倾斜,盆底在后下方。
- [ ] 剖视/底视:盆底排水孔 → 底部储水层 → 溢流孔贯通。
- [ ] 背面干净,无杯体穿出。
- [ ] `echo` 的单元 W×D×H 三维均 ≤ 240mm。
- [ ] `Volumes` 为 2(单一实体)。
- [ ] 导出 STL 成功。
